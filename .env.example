# Paperpile Integration (Required)
# Get this URL from any paper's "Integrations" tab in Paperpile
# Look for "Export all papers as BibTeX" and copy the provided URL
PAPERPILE_EXPORT_URL=https://paperpile.com/eb/your-export-url-here

# Notion Integration (Required)
NOTION_TOKEN=secret_your_notion_integration_token_here
NOTION_DB_ID=your_notion_database_id_here

# Authors Database (Optional)
# AUTHORS_DB_ID=your_authors_database_id_here

# Google Drive Integration (Optional - for PDF linking)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# Processing Options
# LIMIT_MODE=true           # Process only limited entries for testing
# LIMIT_COUNT=10            # Number of entries to process when limited
# TEST_MODE=true            # Parse only, skip Notion operations

# PDF Processing Options
# PDF_MAX_PAGES=25          # Maximum pages to extract from PDFs
# PDF_INCLUDE_PAGE_NUMBERS=false  # Include page numbers in extracted content

# Debug Options (for development)
# EMBED_TEST_MODE=false     # Test PDF content embedding
# MARKDOWN_TEST_MODE=false  # Test markdown conversion
# DRIVE_TEST_MODE=false     # Test Google Drive search
# SINGLE_TEST_MODE=false    # Test single entry processing
# NOTION_TEST_MODE=false    # Test Notion API access
# DEBUG_DRIVE=false         # Debug Google Drive operations
